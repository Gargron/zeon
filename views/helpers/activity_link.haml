%div.format
  %h2
    %a{ :href => "/thread/" + @that.id.to_s }= @that.title
    %small
      %a.tt{ :href => @that.meta["url"], :title => @that.meta["url"], :target => '_blank', :rel => 'nofollow' }= ellipse_url(@that.meta["url"], 80)
  ~ markdownit @that.content